---

mysql_root_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mysql_root_password.txt') }}"

mailman_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mailman_passwd.txt') }}"

list_creators_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/list_creators_passwd.txt') }}"

deliver_user_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/deliver_user_passwd.txt') }}"

rspamd_pw: "{{ lookup('password', 'credentials/' + inventory_hostname + '/rspamd_pw.txt') }}"

mailserver_db_user_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mailserver_db_user_passwd.txt') }}"

tine20_admin_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/tine20_admin_passwd.txt') }}"


