---

mysql_root_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mysql_root_password.txt chars=ascii_letters,digits,hexdigits') }}"

mailman_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mailman_passwd.txt chars=ascii_letters,digits,hexdigits') }}"

list_creators_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/list_creators_passwd.txt chars=ascii_letters,digits,hexdigits') }}"

deliver_user_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/deliver_user_passwd.txt chars=ascii_letters,digits,hexdigits') }}"

rspamd_pw: "{{ lookup('password', 'credentials/' + inventory_hostname + '/rspamd_pw.txt chars=ascii_letters,digits,hexdigits') }}"

mailserver_db_user_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/mailserver_db_user_passwd.txt chars=ascii_letters,digits,hexdigits') }}"

tine20_admin_passwd: "{{ lookup('password', 'credentials/' + inventory_hostname + '/tine20_admin_passwd.txt chars=ascii_letters,digits,hexdigits') }}"


